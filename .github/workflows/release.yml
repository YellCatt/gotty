name: Build and Release

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - os: linux
            arch: mipsle
            suffix: linux_mipsle
            goflags: CGO_ENABLED=0 GOOS=linux GOARCH=mipsle GOMIPS=softfloat GOEXPERIMENT=nofutex
            ldflags: "-s -w -X main.Version=dev-latest -extldflags=-static"
            tags: "netgo,osusergo"
          - os: linux
            arch: amd64
            suffix: linux_amd64
            goflags: CGO_ENABLED=0 GOOS=linux GOARCH=amd64
            ldflags: "-s -w -X main.Version=dev-latest"
            tags: ""
          - os: linux
            arch: arm64
            suffix: linux_arm64
            goflags: CGO_ENABLED=0 GOOS=linux GOARCH=arm64
            ldflags: "-s -w -X main.Version=dev-latest"
            tags: ""
          - os: windows
            arch: amd64
            suffix: windows_amd64
            goflags: CGO_ENABLED=0 GOOS=windows GOARCH=amd64
            ldflags: "-s -w -X main.Version=dev-latest"
            tags: ""

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build for ${{ matrix.target.os }}/${{ matrix.target.arch }}
        run: |
          ${{ matrix.target.goflags }} go build \
            -ldflags="${{ matrix.target.ldflags }}" \
            ${{ matrix.target.tags != '' && format('-tags={0}', matrix.target.tags) || '' }} \
            -o gotty_${{ matrix.target.suffix }}${{ matrix.target.os == 'windows' && '.exe' || '' }}
          ls -la gotty_${{ matrix.target.suffix }}*

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: gotty_${{ matrix.target.suffix }}
          path: gotty_${{ matrix.target.suffix }}*
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List downloaded artifacts
        run: ls -la artifacts/

      - name: Create or Update Dev Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="dev-latest"
          echo "使用固定版本 tag: $VERSION"

          gh release delete "$VERSION" --yes 2>/dev/null || true

          gh release create "$VERSION" \
            --title "Development Build (Latest)" \
            --notes "Automatic build from commit ${{ github.sha }}%0A%0A**Platforms:**%0A- Linux amd64%0A- Linux arm64%0A- Linux mipsle (极路由2 - CGO 已禁用，纯静态/softfloat 编译)%0A- Windows amd64" \
            --prerelease \
            artifacts/*